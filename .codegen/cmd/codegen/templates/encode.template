// generated code - ** DO NOT EDIT **

package encode

import (
   "slices"
   "testing"
)

{{ range $name, $rq := . }}
func Test{{ titleCase $name }}Request(t *testing.T) {
    expected := []byte{
{{ hex $rq.Expected }}
    }

    packet,err := {{ titleCase $rq.Func }}({{- args $rq.Args }})

    if err != nil {
        t.Fatalf("%v",err)
    } else if !slices.Equal(packet, expected) {
        t.Errorf("{{ $name }}: incorrectly encoded request")        
    }
}
{{ end -}}